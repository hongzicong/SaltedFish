package com.ping.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import hongzicong.saltedfish.db.BooleanConverter;
import java.util.List;

import hongzicong.saltedfish.model.EveryDayTask;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "EVERY_DAY_TASK".
*/
public class EveryDayTaskDao extends AbstractDao<EveryDayTask, Long> {

    public static final String TABLENAME = "EVERY_DAY_TASK";

    /**
     * Properties of entity EveryDayTask.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Detail = new Property(1, String.class, "detail", false, "DETAIL");
        public final static Property MBeginTime = new Property(2, long.class, "mBeginTime", false, "M_BEGIN_TIME");
        public final static Property MEndTime = new Property(3, long.class, "mEndTime", false, "M_END_TIME");
        public final static Property IsComplete = new Property(4, boolean.class, "isComplete", false, "IS_COMPLETE");
        public final static Property Name = new Property(5, String.class, "name", false, "NAME");
        public final static Property KeepClockList = new Property(6, String.class, "keepClockList", false, "KEEP_CLOCK_LIST");
        public final static Property IsDetailTime = new Property(7, boolean.class, "isDetailTime", false, "IS_DETAIL_TIME");
        public final static Property Combos = new Property(8, int.class, "combos", false, "COMBOS");
    }

    private final BooleanConverter keepClockListConverter = new BooleanConverter();

    public EveryDayTaskDao(DaoConfig config) {
        super(config);
    }
    
    public EveryDayTaskDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"EVERY_DAY_TASK\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"DETAIL\" TEXT," + // 1: detail
                "\"M_BEGIN_TIME\" INTEGER NOT NULL ," + // 2: mBeginTime
                "\"M_END_TIME\" INTEGER NOT NULL ," + // 3: mEndTime
                "\"IS_COMPLETE\" INTEGER NOT NULL ," + // 4: isComplete
                "\"NAME\" TEXT NOT NULL ," + // 5: name
                "\"KEEP_CLOCK_LIST\" TEXT," + // 6: keepClockList
                "\"IS_DETAIL_TIME\" INTEGER NOT NULL ," + // 7: isDetailTime
                "\"COMBOS\" INTEGER NOT NULL );"); // 8: combos
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"EVERY_DAY_TASK\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, EveryDayTask entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String detail = entity.getDetail();
        if (detail != null) {
            stmt.bindString(2, detail);
        }
        stmt.bindLong(3, entity.getMBeginTime());
        stmt.bindLong(4, entity.getMEndTime());
        stmt.bindLong(5, entity.getIsComplete() ? 1L: 0L);
        stmt.bindString(6, entity.getName());
 
        List keepClockList = entity.getKeepClockList();
        if (keepClockList != null) {
            stmt.bindString(7, keepClockListConverter.convertToDatabaseValue(keepClockList));
        }
        stmt.bindLong(8, entity.getIsDetailTime() ? 1L: 0L);
        stmt.bindLong(9, entity.getCombos());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, EveryDayTask entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String detail = entity.getDetail();
        if (detail != null) {
            stmt.bindString(2, detail);
        }
        stmt.bindLong(3, entity.getMBeginTime());
        stmt.bindLong(4, entity.getMEndTime());
        stmt.bindLong(5, entity.getIsComplete() ? 1L: 0L);
        stmt.bindString(6, entity.getName());
 
        List keepClockList = entity.getKeepClockList();
        if (keepClockList != null) {
            stmt.bindString(7, keepClockListConverter.convertToDatabaseValue(keepClockList));
        }
        stmt.bindLong(8, entity.getIsDetailTime() ? 1L: 0L);
        stmt.bindLong(9, entity.getCombos());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public EveryDayTask readEntity(Cursor cursor, int offset) {
        EveryDayTask entity = new EveryDayTask( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // detail
            cursor.getLong(offset + 2), // mBeginTime
            cursor.getLong(offset + 3), // mEndTime
            cursor.getShort(offset + 4) != 0, // isComplete
            cursor.getString(offset + 5), // name
            cursor.isNull(offset + 6) ? null : keepClockListConverter.convertToEntityProperty(cursor.getString(offset + 6)), // keepClockList
            cursor.getShort(offset + 7) != 0, // isDetailTime
            cursor.getInt(offset + 8) // combos
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, EveryDayTask entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDetail(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setMBeginTime(cursor.getLong(offset + 2));
        entity.setMEndTime(cursor.getLong(offset + 3));
        entity.setIsComplete(cursor.getShort(offset + 4) != 0);
        entity.setName(cursor.getString(offset + 5));
        entity.setKeepClockList(cursor.isNull(offset + 6) ? null : keepClockListConverter.convertToEntityProperty(cursor.getString(offset + 6)));
        entity.setIsDetailTime(cursor.getShort(offset + 7) != 0);
        entity.setCombos(cursor.getInt(offset + 8));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(EveryDayTask entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(EveryDayTask entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(EveryDayTask entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
